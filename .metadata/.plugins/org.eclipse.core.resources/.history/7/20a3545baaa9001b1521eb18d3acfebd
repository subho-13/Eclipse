package gbfs;

import model.Model;
import model.State;

public class GBFS {
    public static <T extends State<T>> void solve(Model<T> model) {
        final Set<T> exploredStates = new TreeSet<T>();
    }
}
